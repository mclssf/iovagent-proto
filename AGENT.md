# IOVAgent 项目智能体规则

本文件适用于仓库根目录及全部子目录。后续智能体在分析、修改、验证和交付本项目时必须遵守。若用户当前请求与本文件冲突，以用户当前请求为准；不要依据历史需求恢复已经删除或替换的功能。

## 1. 项目定位

- 本项目是面向物流运营场景的 ToB 前端演示原型，核心产品为“物流通用智能体”。
- 用户侧核心路径为 `/index/agent`，运营配置后台为 `/agent-ops`，默认入口 `/` 跳转到 `/login`。
- 登录页品牌文案可以使用“大卡数字人”；用户侧左上角产品标题保持“物流通用智能体”。两者属于不同产品表面，不得无需求互相替换。
- 产品同时支持两种工作上下文：
  - 自由对话：不绑定项目，不自动读取项目或企业私有数据。
  - 项目工作台：承载数据源、数据员工、项目技能、运单和在途业务上下文。
- 这是演示原型。模拟流程应真实、完整、可操作，但不得伪装成已经接通的生产服务；除非用户明确要求，不新增真实外部副作用。

## 2. 需求与事实来源优先级

按以下顺序判断产品事实：

1. 用户当前回合的明确要求。
2. 当前分支已经运行的代码与界面行为。
3. `outputs/` 中对应版本的产品需求文档。
4. `docs/` 中的中长期产品材料。
5. Git 历史提交与更早的会话需求。

历史记录只用于理解背景。若当前代码已将名称、入口、菜单或布局替换，不得因为旧文档仍存在而恢复旧状态。

## 3. 固定产品术语

- 使用“数据员工”，不要改成“采集机器人”或其他同义词。
- 使用“在途专家”，不要恢复“物流专家”。
- 使用“运营助手”，不要恢复“运营协同”。
- 使用“经营分析参谋”，不要恢复“智数分析专家”。
- “长期任务”和“下载”属于公共功能，不属于项目内菜单。
- “新对话”是自由对话入口；“新建”与“管理”位于项目区标题右侧。
- 运营后台的通用 Skill 管理不包含 TMS、客户系统接入或数据映射类 Skill；这些能力归属“数据员工配置”。
- 修改产品术语时，应在用户侧、运营侧、筛选项、卡片分类、演示文案和需求文档中检查一致性，但不要改写历史版本文档，除非用户明确要求。

## 4. 主要代码边界

| 路径 | 职责 |
| --- | --- |
| `src/pinia/agentWork.ts` | 用户侧演示数据、会话状态、意图触发、延迟执行、项目与下载状态 |
| `src/views/AgentWork/views/agent.view.vue` | 智能体工作台消息流、输入框、推荐指令和动态右栏 |
| `src/views/AgentWork/component/menu.comp.vue` | 左侧栏、公共功能、最近对话、项目折叠菜单和用户区 |
| `src/views/AgentWork/views/projectCreate.view.vue` | 新建/编辑项目与数字人技能选择、登录验证流程 |
| `src/views/AgentWork/views/orderList.view.vue` | 运单及异常运单列表 |
| `src/views/AgentWork/views/orderDetail.view.vue` | 运单详情、真实地图、轨迹和事件时间线 |
| `src/views/AgentOpsConfig.vue` | 数据员工、Skill、System Prompt、TMS 同步客户等运营配置 |
| `src/views/AgentWork/interface.ts` | 用户侧共享类型；新增消息或结果能力时先扩展类型 |
| `public/demo/` | 可部署、可下载或可在 iframe 中加载的演示资源 |
| `outputs/` | 用户明确要求生成的需求、评审或体验地图等交付物 |

保持职责集中。不要在 Vue 页面和 Pinia Store 中分别维护两套同名业务数据；跨页面共享状态优先放在 Store，纯展示数据留在对应页面。

## 5. 左侧栏与工作区不变量

- 左侧栏分为五个区域：产品标题、公共功能、最近对话、项目、当前用户。
- 产品标题和“新对话”固定在顶部；长期任务、下载、最近对话和项目位于同一可滚动区域；当前用户固定在底部。
- 点击“新对话”只进入空对话；首次发送有效内容后才写入最近对话。
- 自由对话与项目模式切换前应保存当前自由对话消息，不得把项目欢迎信息写入自由对话。
- 项目点击后展开项目内菜单，再次点击同一项目收起；不得重复创建页面或叠加路由。
- 项目内菜单仅包含项目业务页面，例如智能体工作台、运单列表、异常运单列表、运单详情与地图。
- 新建项目与编辑项目必须保持字段、技能分类、选择规则、登录流程和状态回显一致；两者最终确认按钮统一使用“完成”。
- 用户取消新建或编辑后，应返回进入前状态，不改变当前会话、项目或页面上下文。

## 6. 智能体工作台布局不变量

- 所有会话默认不展示右栏；右栏仅在外部 H5 或用户明确要求的动态子页面打开时出现。
- 外部 H5 未打开时，不展示“隐藏右栏”按钮。
- 右栏关闭后恢复单栏，不清空当前会话、输入内容或执行结果。
- 无右栏时，对话与输入区域最大宽度为 `1000px`；右栏打开时最大宽度为 `800px`。
- 智能体消息左侧与输入框左边界对齐；用户消息右侧与输入框右边界对齐。
- 标题栏高度、左右栏标题字号和图标垂直对齐必须一致。标题保持紧凑，不添加无必要副标题。
- 输入框保留文字输入、文件上传、拖入、已上传文件删除、模型选择和发送能力。
- 输入框聚焦时使用现有蓝色外框反馈；推荐指令保持在输入框上方的一行内，三个按钮尽量占满可用宽度，长文本省略。

## 7. 会话演示流程规则

### 7.1 意图识别

- 先识别最具体的意图，再处理泛化关键词；每个命中分支完成后立即 `return`，避免同一输入触发多个流程。
- 关键词匹配应结合动作、对象和业务意图，不要仅凭一个宽泛词触发。例如邮件流程需出现邮件语义或“邮箱地址 + 发送动作”。
- 车辆、运单、订单、货单和车牌号等标识必须保留用户输入值与类型，不得在回复中擅自替换或统一成另一种编号。
- 新增触发规则时，检查与文件填表、MCP 测试、分析报告、车辆定位、预警、异常停车、下载等已有分支的优先级冲突。

### 7.2 执行过程

- 复杂任务采用“用户消息 → 处理中消息 → 分步执行 → 完整结果 → 可选链接/文件”的闭环。
- 使用现有 `startDelayedAgentProcess` 或同类机制模拟真实执行；每一步应说明正在读取什么数据、调用什么能力、得到什么判断。
- 组件卸载、切换请求或新任务开始时清理旧计时器，防止旧结果写入新会话。
- 最终结果必须在会话中提供可独立理解的文字版内容，不能只写“已处理完成”或只依赖右栏。
- 业务结果至少包含：结论、关联运单/车牌、关键事实或证据、风险等级、建议动作。数量和明细必须互相一致。

### 7.3 常用演示场景

- 在途预警：给出真实高风险清单、剔除或降级结果、判断依据和处置优先级。
- 异常停车：区分合理停车与真实风险停车，提供时间、地点、时长、规则、智能体判断和建议。
- 车辆定位：会话中先给出 POI、坐标、定位时间、航向、速度和状态，再提供动态 H5 链接。
- 分析报告：调用“经营分析参谋”和“大卡鹰眼”的演示步骤，报告标题和内容应与用户问题对应。
- 智能填表：输出真实可下载文件；声明 128 行时，文件必须确有 128 行数据，并保留完整运单字段。
- 邮件发送：前端原型只模拟整理、校验、发送和回执；除非用户明确要求并提供后端能力，不实际向外部邮箱发送。
- 推荐指令必须复用输入框正常发送路径，不能维护独立且不同的结果逻辑。

## 8. 外部 H5 与地图规则

- 外部 H5 优先使用项目内同源静态页或受控业务地址，避免固定官网、禁止 iframe 或不稳定第三方页面导致部署失败。
- 动态 H5 参数应来自本次会话结果，例如车牌、运单、POI、经纬度、航向、速度和定位时间。
- 车辆定位 H5 至少展示真实地图底图、当前车辆图标、详细 POI、航向、速度、定位时间和坐标。
- 全项目地图统一使用高德地图 JS API 2.0，包括底图、标记、轨迹、路线规划、围栏、信息窗和地图控件；不得再引入 Leaflet、OpenStreetMap、OSRM 或高德瓦片直链。
- 运单轨迹页应使用高德官方底图；需要展示路线时，使用 `AMap.Driving` 拟合道路。业务 GPS/WGS84 坐标进入地图前使用 `AMap.convertFrom` 转换为高德坐标。
- 不在页面上展示“真实底图”“OSRM 道路拟合”等实现标签。
- 高德 API Key 通过前端环境变量注入，安全密钥仅保存在服务端环境变量中；JS API 服务请求统一经过一级路由 `/_AMapService` 的同源安全代理，部署域名必须加入高德 Key 的安全域名白名单。
- 高德加载器或底图服务失败时提供明确降级状态，不能让空白地图遮住业务文字结果。
- 地图初始化、路由切换和组件卸载时正确创建、刷新和销毁实例，避免重复地图、空白画布和内存泄漏。

## 9. 数据一致性与 Mock 质量

- Mock 数据应符合物流常识：时间先后合理、里程和进度可推导、预计到达晚于当前位置时间、装卸货地与线路一致。
- 汇总数字应从同一份明细数据推导；若为固定演示数字，也要保证总量、风险数、列表条数和文案口径一致。
- 同一运单或车辆跨会话、右栏、地图和下载文件时，运单号、车牌号、线路、POI 和风险结论保持一致。
- 用户输入明确标识时，回复不得生成另一个标识替代。缺少查询对象时先追问，不要静默使用默认车辆。
- 随机数据只用于增强演示变化，必须限制在合理范围；需要回放或自动测试的场景优先使用确定性数据。
- 任何下载卡片必须对应真实存在的 `public/` 文件；新增文件后确认生产构建会复制该资源。

## 10. 项目技能与运营后台联动

- 数据员工和通用 Skill 是两类配置：
  - 数据员工负责连接 TMS/客户系统、登录验证、抓取和数据映射。
  - 通用 Skill 负责在途专家、运营助手、经营分析参谋、运力与货源等能力。
- 数据员工支持全部企业或指定企业可见；指定企业模式支持搜索、多选并至少选择一家企业。
- 通用 Skill 支持名称、分类、描述、可见范围、文件、启停、更新时间和更新用户。
- 用户侧新建/编辑项目只展示当前企业可见且可用的配置；运营侧名称、分类和描述应与用户侧卡片一致。
- 编辑项目必须回显当前技能、数据员工、登录/连接状态和项目统计，不得静默替换失效项。
- System Prompt 只影响更新后的新模型调用；不得回写历史消息或改变已经开始的任务。
- TMS 同步客户列表中的账号密码属于敏感信息。新增展示或日志时默认脱敏，不把明文写入代码、文档、控制台或截图。

## 11. UI 实现准则

- 以当前 Codex/Doubao 风格的三段式工作区为视觉基线：左栏较深的中性底色、中栏柔和内容底色、右栏白色结果面，接缝使用细边框和克制阴影。
- 不恢复独立顶部标题栏，不新增营销式 Hero、装饰性渐变、光球或大面积品牌色背景。
- 运营工具优先高信息密度、清晰层级和重复操作效率。不要把页面区块都做成悬浮卡片，也不要出现卡片嵌套卡片。
- 单个可重复条目、弹窗和真正需要框定的工具可以使用卡片；默认圆角不超过 `8px`，除非沿用输入框等既有组件样式。
- 优先复用 `Icon`、`strokeIconPaths`、Element Plus 和已有控件，不手绘重复 SVG，不使用 emoji 代替功能图标。
- 图标按钮提供 `aria-label` 或 `title`；表单具备标签、禁用、加载、错误、空态和取消路径。
- 标题、图标、数字和按钮在桌面与窄屏下不得溢出、遮挡或导致布局跳动。
- 用户要求“保持其他不变”时，只修改指定组件、状态和文案，不顺带重构颜色、间距或数据结构。

## 12. 编码约定

- 使用 Vue 3、TypeScript、Pinia、Vue Router、Element Plus、Tailwind CSS 4 和高德地图 JS API 2.0 的现有写法。
- Vue 页面优先使用 `<script lang="ts" setup>` 与 Composition API；共享状态使用 Pinia actions/getters。
- 遵循当前代码格式：两空格缩进、单引号、分号、类型导入使用 `import type`、内部路径优先使用 `@/` 别名。
- 新增消息结构、链接、文件或状态字段时，先更新 `interface.ts`，不要使用无约束的 `any`。
- 意图提取、格式化、Mock 生成等逻辑优先写成小型纯函数；只有跨页面状态才进入 Store。
- 不引入新依赖来解决已有 Vue、Element Plus、高德地图 JS API 2.0 或浏览器标准 API 能完成的问题。
- 不修改 `dist/` 中的构建产物；源文件和 `public/` 才是可编辑事实来源。
- 不处理与需求无关的 `.DS_Store`、历史输出、生成文件或元数据。

## 13. 安全与外部服务

- API 密钥、EyUN 应用密钥、ngrok token、账号密码和验证码只放在未提交的环境文件或安全配置中。
- 不把 `.env`、`.env.local` 中的秘密复制到代码、测试、文档、提交信息或最终回复。
- 前端演示中的“发送邮件”“发送短信”“标记处理”等动作默认是本地状态模拟；接入真实服务必须由用户明确要求。
- 当分支中存在 `server/` 微信服务时，保持目标联系人、回调校验、去重和发送逻辑集中在后端；不要把密钥或真实发送逻辑迁到前端。
- 微信查询类回复先确认查询对象；订单号、运单号、货单号和车牌号与用户输入保持一致。多条消息之间的延迟应可测试、可注入。

## 14. 验证要求

每次实现后按改动范围验证：

1. 运行 `pnpm build`，不得留下 TypeScript 或构建错误。
2. UI 改动启动 `pnpm dev`，使用实际输出端口，不假定固定为 `5173`。
3. 至少检查受影响路由；工作台通常检查 `/index/agent`，运营配置检查 `/agent-ops`。
4. 复杂会话流程必须从输入或推荐指令真实触发，等待过程完成后核对文字结果、链接、文件和右栏。
5. 地图或 H5 同时检查桌面和移动宽度，确认底图、车辆/事件标记、弹窗、POI 和控件均可见，无水平溢出。
6. 检查浏览器控制台，不交付新增的 error/warn。
7. 若分支存在后端脚本，运行对应 `server:check`、`server:test` 和健康检查。
8. 运行 `git diff --check`，确认无空白错误；最后报告未运行或无法运行的验证项。

## 15. Git 与交付

- 开始前执行 `git status --short --branch`，确认当前分支和用户未提交改动。
- 不覆盖、不回退、不整理用户已有改动；遇到相关改动时在其基础上继续。
- 仅在用户明确要求时创建/切换分支、提交、合并或推送。分支名按用户原文创建，不擅自加前缀。
- 提交前再次确认改动范围，只提交本次任务相关文件。
- 产品需求、评审材料和体验地图仅在用户要求时写入 `outputs/`，文件名应包含版本或主题。
- 需求文档遵循用户指定结构和字数；不要自动追加背景、总结或实现细节。

## 16. 完成前检查清单

- 当前产品术语是否正确，是否误用已淘汰名称？
- 自由对话、项目上下文和运营配置边界是否被破坏？
- 会话结果是否包含可独立理解的文字，而不是只依赖右栏？
- 所有编号、数量、时间、路线和风险结论是否前后一致？
- 右栏是否仅在需要时出现，关闭后输入框是否恢复宽度？
- 新建与编辑功能是否对齐，取消是否恢复进入前状态？
- 外部 H5、地图和下载资源在生产构建中是否真实存在？
- 是否无意修改了登录页、运营后台或其他未指定页面？
- 构建、浏览器流程和控制台检查是否通过？
- 是否保留了用户未提交改动，且没有执行未经要求的 Git 操作？
